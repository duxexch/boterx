# 💎 تعويض 100% — دليل المشروع الكامل

> **آخر تحديث:** 30 يوليو 2026 · **المشروع:** Boterx
> **المسار:** `C:\Users\gnz\Downloads\bot2\bot`
> **المستودع:** `github.com/duxexch/boterx`
> **السيرفر:** VPS `69.169.108.197` (Ubuntu 24.04, systemd, auto-update cron)

---

## 1. هوية المشروع

| الحقل | القيمة |
|-------|--------|
| **الاسم** | Boterx — بوت مالي على تيليجرام |
| **النوع** | خدمات مالية (إيداع/سحب/مطابقة P2P/تعويض/شكاوى) |
| **اللغة** | Python 3.12+ |
| **لغات الواجهة** | 17 لغة (ar, en, fr, es, de, it, pt, ru, zh, tr, ur, hi, fa, id, ja, ko, th) |
| **نقطة الدخول** | `comprehensive_bot.py` (~13,000 سطر — كلاس واحد `ComprehensiveDUXBot`) |
| **التخزين** | ملفات CSV (ترميز utf-8-sig، قفل threading.Lock لكل ملف) |
| **API** | urllib.request خام (long-polling) |
| **المكتبات** | `python-dotenv`, `openpyxl` |

---

## 2. ملفات الكود

### 2.1 الإنتاج

| الملف | السطور | الوظيفة |
|------|-------|---------|
| `comprehensive_bot.py` | ~13,000 | **البوت الرئيسي** — كل الميزات في ملف واحد |
| `svrp.py` | ~1,440 | 💎 نظام التعويض — SVRPManager (أرصدة، محافظ، شركات، مكافآت، استرداد) |
| `matching.py` | ~600 | 🔄 مطابقة P2P — MatchManager (طلبات، مطابقات، دردشة، تقييم، نزاعات) |
| `theme_config.py` | ~230 | 🎨 نظام الثيمات (3 ثيمات: ذهبي، محيطي، ملكي) |
| `multi_bot.py` | ~310 | 🤖 إدارة بوتات متعددة |

### 2.2 الترجمات

| الملف | المفاتيح | الوظيفة |
|------|------|---------|
| `i18n/*.json` | ~280 لكل لغة | 17 ملف ترجمة |

### 2.3 ملفات البيانات (CSV)

| الملف | الوظيفة |
|------|---------|
| `users.csv` | حسابات المستخدمين (telegram_id, name, phone, customer_id, language, currency, is_banned) |
| `transactions.csv` | كل المعاملات (deposit/withdraw, status, amounts, currency) — **لا تنتهي صلاحيتها** |
| `companies.csv` | الشركات (id, name, type, details, is_active, icon, address, **affiliate_link**) |
| `payment_methods.csv` | وسائل الدفع لكل شركة |
| `complaints.csv` | الشكاوى — **لا تنتهي صلاحيتها** |
| `svrp_credits.csv` | أرصدة التعويض (keep/shared, pending/active/used) — **لا تنتهي صلاحيتها** |
| `svrp_wallets.csv` | محافظ التعويض (balance, frozen, available, wagering) |
| `svrp_companies.csv` | شركات الاسترداد (قديم — غير مستخدم، النظام يقرأ من companies.csv) |
| `user_company_accounts.csv` | حسابات المستخدمين في الشركات (locked) |
| `bonus_requests.csv` | طلبات المكافآت (admin approval) — **لا تنتهي صلاحيتها** |
| `recovery_requests.csv` | طلبات الاسترداد بلقطة شاشة |
| `bot_tokens.csv` | بوتات متعددة |
| `system_settings.csv` | إعدادات النظام |
| `button_labels.csv` | تسميات الأزرار القابلة للتعديل |

---

## 3. البنية المعمارية

### 3.1 توجيه الرسائل (process_message)

```
process_message(message)
│
├── 1. فحص الحالات قبل rate limiter
│   ├── phone_login_waiting → handle_phone_login()
│   ├── choosing_start_language → handle_start_language_choice()
│   ├── start_phone_input → handle_start_phone()
│   ├── registering* → handle_registration()
│   ├── svrp_enter_account_* → تسجيل حساب شركة (خطوة واحدة)
│   ├── svrp_bonus_amount_* → أدمن يكتب مبلغ مكافأة
│   ├── svrp_recovery_amount_* → أدمن يكتب مبلغ استرداد
│   ├── svrp_waiting_screenshot → استقبال لقطة شاشة
│   ├── svrp_send_customer → إرسال رصيد (خطوة 1: معرف العميل)
│   ├── svrp_send_amount_* → إرسال رصيد (خطوة 2: المبلغ)
│   ├── svrp_awaiting_send → redirect إلى svrp_send_customer
│   ├── svrp_approve_amount_* → أدمن يكتب مبلغ استرداد
│   ├── svrp_dep_balance_* → إيداع من الرصيد المتاح
│   ├── svrp_edit_intro_* → تعديل نص شرح التعويض
│   ├── app_wizard* → معالج التطبيقات
│   ├── mbot_wizard* → معالج البوتات
│   └── باقي svrp_* → handle_svrp_state() (أكواد ترويجية)
│
├── 2. فحص المستخدم المسجل/المحظور
│
├── 3. أوامر الأدمن النصية
│
├── 4. rate limiter (30/دقيقة لغير الأدمن)
│
├── 5. أزرار القائمة الرئيسية
│
└── 6. handle_admin_actions() (للأدمن)
```

### 3.2 الإدخال خطوة بخطوة

جميع التدفقات تطلب **بيان واحد في كل مرة** مع تحقق:

| التدفق | الخطوات |
|--------|---------|
| السحب | المبلغ ← المحفظة ← المعرف ← الكود |
| مطابقة P2P (بيانات) | المبلغ ← المحفظة ← المعرف |
| مطابقة P2P (كود) | الكود ← المعرف ← المحفظة ← الوسيلة |
| إرسال رصيد مجمد | معرف العميل ← المبلغ |
| الإيداع | شركة ← وسيلة دفع ← محفظة ← مبلغ |

### 3.3 البيانات الزرقاء القابلة للنسخ

كل الأرقام والبيانات في `<code>` مع `👈 اضغط للنسخ`:
- أرقام العمليات (DEP/WTH)
- أرقام المحافظ
- معرفات الحسابات
- أكواد السحب
- المبالغ
- معرفات الطلبات

---

## 4. نظام 💎 التعويض

### 4.1 الشركات وروابط الإفيليت

- `companies.csv` يحتوي عمود `affiliate_link`
- الإدارة: لوحة الأدمن ← إدارة الشركات ← 🔗 تعديل رابط الإحالة
- للعميل: يظهر كزر `🔗 التسجيل في الشركة` (URL button)
- بعد الضغط والتسجيل، يُطلب رقم الحساب

### 4.2 شروط المطالبة بالمكافأة

العميل يجب:
1. **تسجيل حساب** في الشركة ✅
2. **عمل إيداع وخسارته** (مرفوض) ❌

لو طلب المكافأة بدون استيفاء الشروط:
- يظهر الشرط مع ✅/❌
- زر `🏢 تسجيل حساب` يوصله لقائمة الشركات
- زر `💰 إيداع` يوصله للإيداع
- زر `🔙 رجوع`

### 4.3 نظام الرصيد المجمد

- الرصيد المجمد يُقسم 4 أقسام (25% لكل صديق)
- الحد الأدنى: 4 أصدقاء لفك التجميد الكامل
- عند إرسال X لصديق ← يُفك تجميد X من رصيدك
- إرسال رصيد: خطوة بخطوة (معرف العميل ← المبلغ)

### 4.4 لوحة إدارة التعويض (للأدمن)

```
💎 إدارة التعويض
├── ⚙️ الإعدادات (تعديل كل قيم النظام)
├── 👥 المحافظ (عرض كل المحافظ)
├── 🏢 الشركات والحسابات (عرض الشركات + روابط الإفيليت)
├── 🏆 طلبات المكافآت المعلقة
├── 📸 طلبات الاسترداد (بلقطة شاشة)
├── 💰 طلبات الإيداع من الرصيد المتاح
├── 📝 تعديل النصوص (17 لغة)
└── 📊 إحصائيات تفصيلية
```

### 4.5 انتهاء الصلاحية — معطل

- `cleanup_old_transactions()` → معطل
- `expire_old_credits()` → معطل
- كل الطلبات (إيداع، سحب، شكاوى، مكافآت، استرداد) تبقى معلقة حتى يرد الأدمن

---

## 5. نظام المطابقة P2P

- مطابقة آلية بين طلبات عكسية (إيداع ↔ سحب)
- أسماء وهمية (عميل-XXXX)
- كود السحب يصل للأدمن فقط
- دردشة بين الطرفين + تقييم إجباري + فض نزاعات
- الإدخال خطوة بخطوة (الكود ← المعرف ← المحفظة ← الوسيلة)
- الأدمن يمكنه أن يكون الطرف الآخر عند عدم وجود مطابقة

---

## 6. النشر على السيرفر

| العنصر | القيمة |
|--------|--------|
| **VPS** | `69.169.108.197` (Ubuntu 24.04) |
| **الخدمة** | systemd `boterx.service` |
| **التحديث التلقائي** | cron يفحص GitHub كل دقيقة |
| **Python** | 3.12.3 عبر venv في `/opt/bot/venv` |
| **BOT_TOKEN** | `8909512324:AAGWTHWvtHuFXc9TEQ-TNMmbqCsG8fq5Ap0` |

---

## 7. نقاط تقنية مهمة

- **ترميز CSV:** utf-8-sig دائماً
- **كتابة آمنة:** `safe_csv_write`/`safe_csv_read` مع `threading.Lock`
- **المبالغ:** استخدم `format_amount_with_currency()` — لا تكتب العملة يدوياً
- **الأزرار:** استخدم `transform_keyboard()`/`normalize_button_text()` للنظام
- **Broadcasts:** بدون keyboard
- **تنظيف الحالة:** امسح `user_states` بعد إكمال أي تدفق
- **فحوص الحالة المحددة** قبل الفحص العام `svrp_` لتجنب التقاط خاطئ
