# دليل المطابقة P2P — Boterx

> آخر تحديث: 30 يوليو 2026

---

## 📌 نظرة عامة

نظام P2P يربط بين عميلين: واحد يريد **إيداع** والآخر يريد **سحب** نفس المبلغ من نفس الشركة. النظام يطابقهما آلياً بأسماء وهمية، والأدمن يتحكم في التأكيد.

---

## 🔧 كيف يعمل

### تدفق العميل (خطوة بخطوة):
1. يضغط 🔄 مطابقة → يرى شرح + قواعد + تحذيرات
2. يوافق ✅ → يختار نوع (إيداع/سحب)
3. يختار الشركة (inline buttons)
4. **يُطلب بيان واحد في كل مرة:**
   - المبلغ ← تحقق ← ✅
   - رقم المحفظة ← تحقق ← ✅
   - معرف الحساب ← تحقق ← ✅
5. يُنشأ طلب المطابقة → يبحث عن طرف آخر

### عند العثور على مطابقة:
- كل طرف يحصل على اسم وهمي (عميل-XXXX)
- تُفتح دردشة بينهما
- الساحب يُطلب منه إدخال: الكود ← المعرف ← المحفظة ← الوسيلة (خطوة بخطوة)

### دور الأدمن:
- يصله إشعار بالكود + كل البيانات في `<code>` (قابلة للنسخ)
- أزرار: ✅ تأكيد الكود / ❌ رفض الكود
- يمكن أن يكون الطرف الآخر عند عدم وجود مطابقة

### فض النزاعات:
- أي طرف يضغط 🆘 دعم → يفتح نزاع
- الأدمن يرى سجل الدردشة + 3 خيارات: لصالح المودع / لصالح الساحب / إلغاء

---

## 📁 ملفات النظام

| الملف | الوظيفة |
|------|---------|
| `match_requests.csv` | طلبات المطابقة المعلقة |
| `matches.csv` | المطابقات النشطة والمكتملة |
| `chat_messages.csv` | رسائل الدردشة |
| `ratings.csv` | التقييمات (1-5 نجوم) |
| `disputes.csv` | النزاعات |

---

## ⚠️ ملاحظات

- **الكود يصل للأدمن فقط** — لا يصل للطرف الآخر
- **التقييم إلزامي** بعد كل مطابقة
- **لا تنتهي صلاحية** أي طلبات مطابقة
- كل البيانات في `<code>` زرقاء قابلة للنسخ
